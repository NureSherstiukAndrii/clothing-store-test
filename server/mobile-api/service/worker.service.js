const tokenService = require("../../service/tokenService");
const bcrypt = require('bcrypt');
const utilities = require('../utilities/')

class WorkerService {
    async registrateWorker(name, mail, password) {

    }

}

module.exports = new WorkerService();