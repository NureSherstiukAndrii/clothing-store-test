{
  "build": {
    "outputDir": "build",
    "command": "npm run build"
  },
  "routes": [
    {
      "path": "/",
      "command": "npm start",
      "env": {
        "NODE_ENV": "production"
      }
    }
  ],
  "env": {
    "NODE_ENV": "development"
  }
}
